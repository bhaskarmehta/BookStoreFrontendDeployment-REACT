kind: Service
apiVersion: v1 
metadata:
   name: frontend-svc 
   namespace: frontend
spec:
   selector:
        name: frontend-deployment  
   ports:
      - protocol: "TCP"
        port: 5173
        targetPort: 5173     
   type: LoadBalancer    